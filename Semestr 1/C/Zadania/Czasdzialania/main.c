#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <stdbool.h>
#include "Lista6zad2.h"
/*Jakub Kuciński, nr indeksu 309881, Lista zadań 6, zadanie 2., 27.11.2018, grupa środa 10-12, wstęp do programowania w C */


int main()
{
    /*clock_t mini, sredni, maksi;
    int n=100;
    int r=50000;
    czas_dzialania(n, r, &mini, &sredni, &maksi, mojsort);
    printf("%ld %ld %ld \n", mini, sredni, maksi);
    czas_dzialania(n, r, &mini, &sredni, &maksi, quicksort);
    printf("%ld %ld %ld \n", mini, sredni, maksi);*/


    /*int n=5;
    double t[n];
    generator(n, t);
    double posort[n];

    for(int i=0; i<n; i++)
    {
        posort[i]=t[i];
    }
    for(int i=0; i<n; i++)
    {
        printf("%lf ", t[i]);
    }
    printf("\n");
    mojsort(n, posort);
    for(int i=0; i<n; i++)
    {
        printf("%lf ", posort[i]);
    }
    int k=czy_poprawny(n, t, posort);
    printf("\n%d\n", k);

    for(int i=0; i<n; i++)
    {
        posort[i]=t[i];
    }
    quicksort(n, posort);
    for(int i=0; i<n; i++)
    {
        printf("%lf ", posort[i]);
    }
    printf("\n");
    k=czy_poprawny(n, t, posort);
    printf("%d", k);
    return 0;*/
}
